# CMake build output — fully regenerated by cmake -S . -B build
build/

# Python cache
__pycache__/
*.pyc
*.pyo

# Nsight / profiler outputs (keep reports/ folder but ignore large binaries if needed)
*.nsys-rep
*.sqlite
*.ncu-rep
*.bin
*.pdf
